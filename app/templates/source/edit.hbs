<fieldset>
    <legend>settings</legend>
    priority:
    {{view Mediator.RangeField valueBinding=priority type="range" min="-150" max="150"}} <br />
    additional: {{input type=checkbox checked=additional}}
</fieldset>

{{outlet}}