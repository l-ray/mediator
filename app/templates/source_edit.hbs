<fieldset>
    <legend>settings</legend>
    priority: {{input value=priority name="the priority" }} <br />
    additional({{this.additional}}): {{input type=checkbox checked=additional}}
</fieldset>
{{outlet}}
