         {{#if pictures}}
            <ul>
                {{#each picture in pictures}}
                    <li><img {{bind-attr src=picture.src}} /></li>
                {{/each}}
            </ul>
        {{else}}
            <span class="noPicturesWarning">No pictures found for this event.</span>
        {{/if}}
