{{#each items as |item|}}
    <li class="tag {{if allSelected 'allActive'}} {{if item.selected 'active'}}" {{action 'toggleCategory' item}}>
          <span class="tag-{{item.value}}">{{item.key}}</span>
      </li>
  {{/each}}

{{yield}}
