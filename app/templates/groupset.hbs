<div>
    <div class="col-md-3">
        <div class="well sidebar-nav">
            <ul class="list-group">
                {{#each connection in connections}}
                    {{connection-details connection=connection tagName="li" classNames="list-group-item" }}
                {{/each}}
            </ul>
        </div>
    </div>
    <div class="col-md-9">
        <h1>groupSet</h1>

        <dl>
            <dt>event Date:</dt> <dd>{{ date }}</dd>
        </dl>

        {{ render 'groups' groups }}

        {{outlet}}

    </div>
</div>


